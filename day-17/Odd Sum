def Sum_of_odd_numbers(n):
    sum = 0
    curr = 1
    i = 0
    while i < n:
        sum = sum + curr
        curr = curr + 2
        i = i + 1
    return sum
n = int(input())
final_answer = Sum_of_odd_numbers(n)
print(final_answer)
